import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  
    imagecard: {
        width: '30%',
        height: '40%',
        marginTop: 210,
        borderRadius: 10,
    
    }

    
});
export default styles;
