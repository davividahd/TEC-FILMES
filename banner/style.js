import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  
      textBanner: {
        fontSize: 45,
        color: "white",
        marginTop: 15,
        marginLeft: -8,
        fontWeight: "bold"
    
}
 });
export default styles;