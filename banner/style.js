import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  
      textBanner: {
        fontSize: 45,
        color: "white",
        marginTop: 15,

        fontWeight: "bold"
    },
    
    imagebanner: {
        width: 400,
        height: 200,
        marginTop: 10,
        borderRadius: 20,

    },
}
 );
export default styles;
